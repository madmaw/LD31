part of game;

class TileMatch {

  int x;
  int y;
  int length;
  bool vertical;
  double baseValue;
  EntityType entityType;
  
  TileMatch(this.x, this.y, this.length, this.vertical, this.baseValue, this.entityType) {
    
  }

}